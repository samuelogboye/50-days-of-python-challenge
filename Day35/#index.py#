#!/usr/bin/python3


def find_index(a, b):
    """Takes two arguments
          a - list of integers
          b - an integer
    return indexes of integer in the list
    """
    new_list = []
    if b in a:
        for num in a:
            new_list.append(a.index(b))
        return new_list
    else:
        new_list.append(